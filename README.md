# GraphRAG Lab

GraphRAG、检索增强生成与知识图谱研究站点。

## 本地运行

需要 Ruby、Bundler 和 Jekyll 环境：

```bash
bundle install
bundle exec jekyll serve
```

然后访问 `http://localhost:4000/leokongjian.github.io/`。

站点通过 GitHub Actions 自动构建并部署到 GitHub Pages。
